<what-next>
  <status>defined</status>
  <last-updated>2026-08-25</last-updated>

  <tbd-items>
  </tbd-items>

  <next-steps>
    <step>Run tests/probe-p1.py --run to measure whether /breakdown builds the won't-have</step>
  </next-steps>

  <session-notes>
  A probe, not a product. Four features, one per MoSCoW tier, two criteria each.

  The slugs are animals because the probe has to attribute a generated task to the
  feature it came from, and today no task carries a source-feature element -- item 16
  adds that. Until then attribution is by string match, and a string match is only
  sound when the string cannot occur by coincidence. "telemetry" could appear in a
  task about logging; "quokka" could not.
  </session-notes>
</what-next>
